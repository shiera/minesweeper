package UI;

/**
 * Author: Shiera
 * Something that handles button clicks.
 */
public interface ButtonHandler {

    /**
     * does what should happen when button is clicked
     * @param frame  frame used in mineSweeper
     */
    void onButtonClick(BaseFrame frame);
}
